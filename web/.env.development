# 2026-04-17 新增 — 环境区分任务 1：Web 端 development 契约冻结
# 变更类型：新增/配置
# 功能描述：为 web(5174) 冻结与多端一致的 development 基准地址，并保留当前运行时可复用的兼容别名。
# 设计思路：
#   1. 使用 Vite `VITE_` 前缀承载共享语义：APP_ENV / BACKEND / WEB / WEB_HUB / SSO_HUB。
#   2. 保留 `VITE_API_BASE` 作为当前/过渡期兼容别名，后续任务 3 再把运行时读取统一收口到共享契约。
#   3. development 锁定 localhost:3000 / 5174 / 5173，不允许静默回退生产公网地址。
# 参数与返回值：以下均为 Vite build-time 环境变量，无运行时返回值。
# 影响范围：web Vite 开发态 env 注入、后续 backend/skill/SSO 链路接线。
# 潜在风险：任务 1 仅冻结契约，实际消费逻辑将在任务 3 落地。
VITE_APP_ENV=development
VITE_BACKEND_PUBLIC_BASE_URL=http://localhost:3000
VITE_WEB_PUBLIC_BASE_URL=http://localhost:5174
VITE_WEB_HUB_PUBLIC_BASE_URL=http://localhost:5173
VITE_SSO_HUB_BASE_URL=http://localhost:5173
VITE_API_BASE=http://localhost:3000
