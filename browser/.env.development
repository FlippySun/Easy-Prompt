# 2026-04-17 新增 — 环境区分任务 1：Browser 端 development 契约冻结
# 变更类型：新增/配置
# 功能描述：为 browser 开发态冻结与多端一致的 app 基准地址，供后续任务 5 通过 WXT build-time env 注入消费。
# 设计思路：
#   1. 使用 WXT 官方支持的 `WXT_` 前缀，后续通过 `import.meta.env` 读取。
#   2. 明确 3002 仅是 WXT dev server/HMR 端口，不是扩展真实运行时 origin，也不是 OAuth redirect 基准地址。
#   3. development 锁定 backend=3000、web=5174、web-hub/SSO hub=5173，不对 Browser 的真实 callback scheme 做错误简化。
# 参数与返回值：以下均为 WXT build-time 环境变量，无运行时返回值。
# 影响范围：browser/shared/api.js、browser/shared/sso.js、content/options/E2E 的后续环境接线。
# 潜在风险：任务 1 仅冻结契约；实际读取与测试切换将在任务 5 落地。
WXT_APP_ENV=development
WXT_BACKEND_PUBLIC_BASE_URL=http://localhost:3000
WXT_WEB_PUBLIC_BASE_URL=http://localhost:5174
WXT_WEB_HUB_PUBLIC_BASE_URL=http://localhost:5173
WXT_SSO_HUB_BASE_URL=http://localhost:5173
WXT_ZHIZ_SKILLS_MANAGER_URL="https://sit.zhiz.me/chat-flow/#/skills/index"
