# Dependencies
node_modules/
.gradle/
build/
doc/private/
secrets.json
# [2026-03-20][配置变更] Ignore browser extension packaged artifacts | Rationale: dist is local build output for device testing, should not be versioned or pushed
browser/dist/
# [2026-04-13][配置变更] Ignore web Vite build output | Rationale: dist is generated by `npm run build`, deploy.sh copies it to VPS
web/dist/

# IDE
.idea/
*.iml
*.code-workspace
# [2026-04-17][配置变更] Ignore IDE self-update lock | Rationale: local IDE runtime lock has no product/runtime value and should not enter version control
intellij/.intellijPlatform/self-update.lock

# OS
.DS_Store
Thumbs.db

# Build artifacts
*.vsix
*.zip
*.jar
out/

# Secrets
.env
*.key

# Local deploy scripts (tokens, credentials)
deploy/

# [2026-04-23][change type: config][what: ignore local Codex config files][why: repo-local runtime config may contain machine-specific settings and secrets][params & return: affects config.toml path only; no runtime return value][impact scope: prevents accidental Git staging/push of local Codex config from this workspace][risk: No known risks]
# Local AI/MCP tool configs (not for repo)
config.toml
.mcp.json
.mcp.json.*
.vscode/
.roo/
.claude/
.playwright-mcp/
# [2026-04-17][配置变更] Ignore local agent workspace folders | Rationale: agent-generated local workspace metadata is machine-specific and should not be versioned
.agent/
.agents/
.github/copilot-instructions.md
.github/copilot-instructions.md.*
.github/agents/
.github/prompts/
.github/skills/
.github/settings.local.json

# Project analysis reports (local only)
PROJECT_REVIEW_REPORT.md
PROJECT_SUMMARY.md

# WXT local build state
browser/.wxt/
browser/.output/

# [2026-04-23][change type: config][what: keep planning artifacts local-only][why: planning files can include sensitive workflow context and should never be uploaded to the remote repo][params & return: affects top-level .planning/ ignore rule; no runtime return value][impact scope: prevents future Git staging of local planning artifacts in this workspace][risk: Existing tracked .planning files must also be removed from the Git index to fully stop tracking]
# Local planning and workflow artifacts (generated by Claude/GSD)
.planning/
backend/.planing/

# DeerFlow (独立项目，不属于 Easy Prompt 发布物)
deer-flow/

# Changelog and deploy docs (maintained locally, not versioned)
CHANGELOG.md
DEPLOY.md
